trigger: none

pr:
- main
- develop

resources:
  repositories:
    - repository: templates
      type: git
      name: Engineering/api-doc-tools
      ref: danni-update-service-connection

extends:
  template: .pipelines/bootstrap/trigger-Buddy.yml@templates
